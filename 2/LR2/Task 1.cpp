#include <iostream>
#include <math.h>

using namespace std;

int main() {
    //1 приклад
    int x;
    float Y;

    cin >> x;

    if (x <= 0) {
        Y = pow(x, 2 - sin(y));
    }
    else if (x > 0)
    {
        Y = sqrt(x) + cos(y);
    }

    cout << Y;

    return(0);
}